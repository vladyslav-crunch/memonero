import styled from "styled-components";

export const BaseToaster = styled.div``;
export const SuccessToaster = styled(BaseToaster)``;
export const WarningToaster = styled(BaseToaster)``;
export const ErrorToaster = styled(BaseToaster)``;
